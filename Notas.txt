¿Que formas hay para crear elementos HTML?

- Podemos crearlo directamente en el archivo index.html

<div id="home" class="header"> </div>

- Podemos crearlo con createElement y appendChild

//Paso 1: crear el HTML Element
let myDiv = document.createElement('div');
//Paso 2: configurarlo
myDiv.id = "home";
myDiv.classList.add("header");
//Paso 3:
document.body.appendChild(myDiv);

- Usar el InnerHTML

------------------------------------------------------
//Reduce
//Calcular el promedio de peso de todos los pokemons
// suma de todos los pesos / el total de pokemon

let sumaPesos = data.pokemon.reduce(

  (acumulador, pokemon) => {
    acumulador = acumulador + Number.parseFloat(pokemon.size.weight.replace(' kg', ''));
    return acumulador;
  },
  0
)

let totalPokemons = data.pokemon.length; //251
let promedioPesos = sumaPesos / totalPokemons;
console.log(promedioPesos);

